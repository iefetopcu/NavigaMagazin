@using NavigaMagazin.Models.Entity

@foreach (var ebultenlist in Model)
{
    <li class="list-group-item d-flex px-3">
        <span class="text-semibold text-fiord-blue">@ebultenlist.ebultenmail</span>
        <span class="ml-auto text-right text-semibold text-reagent-gray">@ebultenlist.registertime.ToString("dd.MM.yyyy")</span>
    </li>
}